name: Backend Heartbeat

on:
  schedule:
    # Every Sunday at 12:00 UTC
    - cron: "0 12 * * 0"

  workflow_dispatch:

jobs:
  heartbeat:
    runs-on: ubuntu-latest

    steps:
      - name: Ping backend
        run: |
          curl -f https://edutrack-7qla.onrender.com/heartbeat